apiVersion: v1
kind: Service
metadata:
  name: mysql-svc
  labels:
    app.kubernetes.io/instance: db
spec:
  selector:
    app: mysql
  ports:
    - protocol: TCP
      targetPort: 3306
      port: 3306
